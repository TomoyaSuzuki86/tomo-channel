/**
 * Prisma seed scaffold for tomo-channel.
 *
 * This repository is still static-first, so this script intentionally does
 * not connect to a live database yet. When v0.5+ introduces Prisma-backed
 * writes, replace this scaffold with actual Prisma Client inserts.
 */

function main() {
  console.log("Prisma seed scaffold for tomo-channel.");
  console.log("No database writes are performed in v0.4.0.");
  console.log("When ready, populate Article, ArticleSource, Comment, and AiReplyJob rows here.");
}

main();
